module InText: {
  [@react.component]
  let make: (~pairs: list((string, string))) => React.element;
};

module Boxed: {
  [@react.component]
  let make: (~pairs: list((string, string))) => React.element;
};
