module InText: {
  [@react.component]
  let make: (~keyText: string, ~value: string) => React.element;
};

module Boxed: {
  [@react.component]
  let make: (~keyText: string, ~value: string) => React.element;
};
